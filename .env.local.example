# PostgreSQL接続文字列。ローカル開発やVercel以外にデプロイする場合はここに設定してください。
# Vercel + Vercel Postgres / Neon integration を使う場合は自動的に環境変数が設定されるため、
# 通常このファイルをコピーする必要はありません。
POSTGRES_URL=postgres://user:password@localhost:5432/ig_analytics
